
Preciso EXPORTAR COMPLETAMENTE meu projeto atual que está aqui no Replit (front-end + back-end + configurações) e VINCULAR esse projeto a um repositório no meu GitHub. Além disso, preciso gerar um BACKUP COMPLETO do banco de dados usado por este projeto, salvá-lo junto ao código, para que eu consiga subir tudo em um novo ambiente depois.

TAREFAS QUE VOCÊ DEVE REALIZAR / ME GUIAR PASSO A PASSO:

1. DIAGNÓSTICO INICIAL DO PROJETO
   - Identificar:
     - Qual é a stack do front-end (por ex.: React, Next.js, Vue, etc.).
     - Qual é a stack do back-end (por ex.: Node.js/Express, NestJS, Laravel, etc.).
     - Qual banco de dados está sendo usado (por ex.: PostgreSQL, MySQL, SQLite, MongoDB, Supabase, etc.).
   - Mapear TODAS as pastas e arquivos relevantes do projeto (front + back, configs, scripts, Dockerfiles, etc.).
   - Verificar se já existe repositório Git inicializado no projeto (pasta .git). Se não existir, inicializar.

2. CONFIGURAR INTEGRAÇÃO COM O GITHUB
   - Me guiar para conectar o Replete à minha conta do GitHub (autenticação / token / autorização, se necessário).
   - Criar OU usar um repositório no GitHub (me perguntar se quero criar um novo ou apontar para um já existente).
   - Configurar o remote corretamente, por exemplo:
     - `git remote add origin <URL-do-repositorio>`
   - Garantir que nome da branch principal esteja correto (por exemplo: `main`).

3. PREPARAR O CÓDIGO PARA VERSIONAMENTO
   - Garantir que TODO o código do front-end esteja incluído (pasta de frontend, componentes, páginas, assets, configs).
   - Garantir que TODO o código do back-end esteja incluído (API, serviços, configs, middlewares, etc.).
   - Garantir que arquivos importantes de configuração entrem no repositório (por exemplo: Dockerfile, docker-compose, env.example, scripts de build e deploy).
   - Ajustar um `.gitignore` adequado:
     - Ignorar dependências e arquivos gerados automaticamente (ex.: `node_modules`, `.next`, `dist`, `build`, logs, etc.).
     - NÃO ignorar o backup do banco que vamos gerar (ver item do banco).

4. BACKUP COMPLETO DO BANCO DE DADOS
   - Detectar qual banco de dados estou usando neste projeto.
   - Gerar um DUMP/backup completo da base:
     - Se for PostgreSQL, usar algo como `pg_dump`.
     - Se for MySQL/MariaDB, usar algo como `mysqldump`.
     - Se for SQLite, copiar o arquivo `.db`.
     - Se for outro banco, orientar com o comando equivalente.
   - Salvar o backup dentro de uma pasta do projeto, por exemplo: `backup/` ou `db_backup/`.
   - Nomear o arquivo de backup com data/hora (ex.: `backup-YYYYMMDD-HHMM.sql` ou similar).
   - Garantir que esse arquivo de backup seja incluído no Git (não entrar no .gitignore).
   - Criar um pequeno arquivo `README_DB_BACKUP.md` explicando:
     - Tipo de banco.
     - Como restaurar esse backup no novo ambiente (comandos de exemplo).

5. PRIMEIRO COMMIT E PUSH COMPLETO
   - Executar os comandos necessários:
     - `git status` para validar o que será commitado.
     - `git add .` para adicionar todos os arquivos (incluindo front, back e backup do banco).
     - `git commit -m "Exportação completa do projeto (front, back e backup do banco)"`.
     - `git push -u origin main` (ou a branch principal configurada).
   - Se ocorrer qualquer erro (permissão, autenticação, conflito etc.), você deve:
     - Explicar claramente o erro.
     - Me dizer exatamente qual comando rodar ou qual ação tomar para corrigir.

6. VERIFICAÇÃO FINAL
   - Confirmar que:
     - O repositório no GitHub está com TODO o código do front-end e back-end.
     - O backup do banco de dados está presente no repositório (pasta de backup).
     - O README (principal ou específico) contém as instruções mínimas para:
       - Instalar dependências.
       - Subir front-end e back-end.
       - Restaurar o banco de dados num novo ambiente.

FORMATO DE RESPOSTA / INTERAÇÃO:
- Sempre me responda em português do Brasil.
- Diga exatamente quais comandos devo executar, na ordem correta.
- Antes de rodar algo destrutivo ou que possa sobrescrever dados, me avise e peça confirmação.
- Se faltar alguma informação (por exemplo, credenciais do banco ou URL do GitHub), pergunte explicitamente o que precisa para continuar.


Link do projeto https://github.com/Devs-Wescctech/app-politicall
